# Training outputs, not source: trained emulators, the figures made while checking them, and
# the slurm logs of the jobs that produced them. They are large (~90 MB), they are regenerated
# by rerunning the training, and a `git add -A` from the repository root will otherwise sweep
# them all in.
*.npy
*.h5
*.png
_sbatch/
nb/
